# -----------------------------------------------------------------------------
# satori-desktop — environment variables (example)
#
# Copy this file to `.env` (DO NOT COMMIT `.env`) and export these variables in
# your shell, your process manager, or your deployment environment.
# -----------------------------------------------------------------------------
#
# Desktop app (Electron main process)
# Used for authentication and sync over HTTP.
SATORI_API_BASE_URL=http://localhost:4000
#
# Server (Postgres + auth/sync API)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/satori_desktop
#
# JWT signing secret (min 32+ chars recommended). Rotate if compromised.
JWT_SECRET=change-me-to-a-long-random-string
#
# Optional: JWT TTL (default 259200 = 3 days).
JWT_EXPIRES_IN_SECONDS=259200
#
# Optional: server port (default 4000).
PORT=4000
